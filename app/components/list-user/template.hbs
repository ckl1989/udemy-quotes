    <div class="col-sm-8">
                <div class="ilm">
                        <ul class="list-inline m">
                            {{#link-to "user.index" tagName="li" activeClass="active"}}
                                    <span class="small-txt">Quotes</span>
                                    <span class="big-txt">{{user.numberOfQuotes}}</span>
                            {{/link-to}}
                            {{#link-to "user.following" tagName="li" activeClass="active"}}
                                    <span class="small-txt">following</span>
                                    <span class="big-txt">{{user.numberOfFollowing}}</span>
                            {{/link-to}}
                            {{#link-to "user.followers" tagName="li" activeClass="active"}}
                                    <span class="small-txt">followers</span>
                                    <span class="big-txt">{{user.numberOfFollowers}}</span>
                            {{/link-to}}
                        </ul>
                    </div>
                    <div class="form-group">
                        <div class="row">
                            <div class="col-sm-4">
                                <div class="folwr">
                                    <div class="t-line"></div>

                                    {{#link-to "user" class="f-box"}}
                                        <img src="images/gravatar.png" alt="avatar">
                                    {{/link-to}}
                                    <div class="form-group">
                                        <h2>{{user.username}}</h2>
                                        <p>@{{user.username}} - {{user.aboutMe}}</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
</div>